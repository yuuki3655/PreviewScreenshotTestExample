* Run `./gradlew updateScreenshotTest` to update reference image.
  * The reference images are stored in `./app/src/debug/screenshotTest/reference/`
* Run `./gradlew validateScreenshotTest` to run test.
